app:
  ingress:
    host: montandon-etl-stage.monty.org
    tls:
      secretName: montandon-helm-secret-cert

  # Environment variables
  env:
    # Azure configs
    AZURE_STORAGE_ENABLE: "true"
    AZURE_CLIENT_ID: XXXXCLIENT-IDXXXX
    AZURE_TENANT_ID: XXXXTENANT-IDXXXX
    AZURE_STORAGE_MEDIA_CONTAINER: montandon-etl-staging-media-container
    AZURE_STORAGE_STATIC_CONTAINER: montandon-etl-staging-static-container
    AZURE_STORAGE_ACCOUNT_NAME: montystagingXYZ
    AZURE_STORAGE_MANAGED_IDENTITY: "true"
    # ETL Load config
    EOAPI_STAC_API_INTERNAL: "https://montandon-eoapi-stage.monty.org/stac"
    EOAPI_STAC_API_PUBLIC: "https://montandon-eoapi-stage.monty.org/stac"

  serviceAccount:
    annotations:
      azure.workload.identity/client-id : "XXXXCLIENT-IDXXXX"

  secretsStoreCsiDriver:
    parameters:
      clientID: "XXXXCLIENT-IDXXXX"
      keyvaultName: "montandon-etl-staging-kv"
      tenantId: "XXXXTENANT-IDXXXX"
