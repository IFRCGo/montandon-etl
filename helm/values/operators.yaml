# Operator-managed infrastructure (Dragonfly cache + RabbitMQ broker) and the
# env wiring that points the app at those in-cluster services.
# Applied as an overlay (see ../tests.yaml) and at deploy time alongside the
# environment values files.
app:
  # Dict form (keyed by env-var name); values are k8s env specs minus name.
  extraEnvVars:
    # Checkout the resources defined by extraManifests
    CELERY_RABBITMQ_CONNECTION_STRING: { valueFrom: { secretKeyRef: { name: monty-rabbitmq-default-user, key: connection_string } } }
    CELERY_RABBITMQ_VHOST:             { value: celery }
    CELERY_RESULT_BACKEND:             { value: redis://monty-dragonfly:6379/0 }
    CACHE_REDIS_URL:                   { value: redis://monty-dragonfly:6379/1 }

  extraManifests:
    dragonflydb-cluster:
      apiVersion: dragonflydb.io/v1alpha1
      kind: Dragonfly
      metadata:
        labels:
          app.kubernetes.io/name: dragonfly
        name: monty-dragonfly  # NOTE: this will be the service name
      spec:
        replicas: 1
        args: ["--dbfilename=mainSnapshot"]  # NOTE: Only maintains 1 last snapshot
        resources:
          requests:
            cpu: "0.1"
            memory: 1024Mi
          limits:
            cpu: "1"
            memory: 1024Mi
        snapshot:
          cron: "*/5 * * * *"
          persistentVolumeClaimSpec:
            # storageClassName:
            accessModes:
            - ReadWriteOnce
            resources:
              requests:
                storage: 1Gi

    # RabbitMQ https://www.rabbitmq.com/kubernetes/operator/using-operator
    rabbitmq-cluster:
      apiVersion: rabbitmq.com/v1beta1
      kind: RabbitmqCluster
      metadata:
        name: monty-rabbitmq  # NOTE: this is used as the secret name
      spec:
        image: rabbitmq:4.2.6-management
        replicas: 1  # Scaling down is not supported
        resources:
          requests:
            cpu: "100m"
            memory: "2Gi"
          limits:
            cpu: "4"
            memory: "3Gi"
        rabbitmq:
          additionalConfig: |
            default_vhost = celery
            consumer_timeout = 7200000
        persistence:
         # storageClassName:
         storage: "4Gi"
