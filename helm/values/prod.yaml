app:
  environment: PROD
  worker:
    queues:
      # NOTE: Make sure keys are lowercase
      extraction:
        # NOTE: replicas are owned by the KEDA ScaledObject (see values.yaml)
        resources:
          requests:
            memory: 1.5Gi
      transform:
        resources:
          requests:
            memory: 1.5Gi
